version: '3'
services: 
  api:
    build:
      context: .
      dockerfile: ./api/Dockerfile
    volumes:
    - .dockervenv:/src/.venv
    - .:/src
    ports:
      - 8000:8000
    environment:
      - WATCHFILES_FORCE_POLLING=true